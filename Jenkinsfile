pipeline {
    agent any

    stages {
        stage('clone git') {
            steps {
                sh "git clone https://github.com/gergpol1998/docker-spark-cluster.git"
            }
        }

    }
}
